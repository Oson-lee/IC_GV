# Giant Virus Classification — Python dependencies
#
# Install (CPU):
#   pip install -r requirements.txt
#
# For GPU/CUDA, install PyTorch from https://pytorch.org/get-started/locally/
# first, then install the remaining packages:
#   pip install -r requirements.txt --no-deps torch
#   (or omit torch from this file and pip install the rest)

# --- Deep learning ---
torch>=2.0.0

# --- Data & numerics ---
numpy>=1.24.0
pandas>=2.0.0

# --- Machine learning ---
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0

# --- Bioinformatics ---
biopython>=1.81

# --- Metadata (metadata.xlsx) ---
openpyxl>=3.1.0

# --- Reporting (evaluate_dl.py: DataFrame.to_markdown) ---
tabulate>=0.9.0
